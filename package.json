{
  "name": "baize",
  "version": "0.0.1",
  "description": "Local-first multi-repo code Q&A over your GitLab fleet",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lukaisluka/baize.git"
  },
  "bin": {
    "baize": "src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "ui"
  ],
  "files": [
    "src",
    "ui/dist"
  ],
  "scripts": {
    "start": "node src/cli.js",
    "build:ui": "npm run build -w ui",
    "prepack": "npm run build:ui",
    "test": "node --test test/*.test.js",
    "test:ui": "npm run test -w ui",
    "benchmark:run": "node src/benchmark/run-cli.js",
    "benchmark:report": "node src/benchmark/report-cli.js"
  },
  "dependencies": {
    "codebase-memory-mcp": "0.10.8",
    "ws": "^8.21.3"
  }
}
